import { SuccessResponseInterceptorInterceptor } from './success-response-interceptor.interceptor';

describe('SuccessResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new SuccessResponseInterceptorInterceptor()).toBeDefined();
  });
});
